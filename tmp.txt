conda create -n assistant python=3.10 -y
conda activate assistant
pip install -r requirements.txt

docker compose -f docker-compose-gpu.yml up -d
# 查看镜像
docker compose images
# 删除所有镜像
docker compose down --rmi all -v

# 通过镜像ID删除
docker rmi 镜像ID

docker ps
# 进入docker内部
docker exec -it ragflow-server bash
nvidia-smi
pip install nvitop
nvitop
exit

ragflow/docker/docker-compose-gpu.yml
修改 count 从 all 改为 1

# 查看所有端口及对应的进程
netstat -tulnp

# 查看特定端口（例如10081）的进程
netstat -tulnp | grep 10081

# 基本用法，发送终止信号
kill 1234

# 强制终止（如果正常终止失败）
kill -9 1234